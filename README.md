## The default coordinate system is:

- Origin (0,0) → Top-Left corner of the SurfaceView
- +X axis → toward the Right
- +Y axis → toward the Bottom
- Units are pixels


(0,0) ───────────► +X
│
│
▼ +Y
